<p id="notice"><%= notice %></p>

<p>
  <b>Image name:</b>
  <%= @image.image_name.file.filename %>
</p>
  <%= image_tag @image.image_name_url %>
  <br />
  <%= @image.tag_list %>
  <br />

  <br />

   Calculated Readability: <%= @image.get_reg_val %>
   MLDS Readability: <%= @image.results.average(:psi) %>

<%= link_to 'Edit', edit_image_path(@image) %> |
<%= link_to 'Back', images_path %>
