<%= simple_form_for(@experiment) do |f| %>
  <% if @experiment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@experiment.errors.count, "error") %> prohibited this experiment from being saved:</h2>

      <ul>
      <% @experiment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <%= f.input :name  %>
    <%= f.input :tag_list  %>
    <%= f.input :image_ids, as: :check_boxes, collection: Image.all, label_method: :image_name_tag%>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
